---
- name: configure a server for mail with Postfix
  hosts:
  remote_user: root
  gather_facts: no

  vars_files:
    - vars/main.yml

  pre_tasks:
    - name: Force install python2
      raw: sudo apt-get -y install python-simplejson

  roles:
    - common
    - zzet.rbenv

    # TODO
